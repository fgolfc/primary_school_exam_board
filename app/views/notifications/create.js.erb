if ("<%= escape_javascript(@notification.errors.full_messages.to_sentence) %>" == "") {
    alert("通知が成功しました");
    // 通知フォームやボタンの状態をリセットするJavaScriptコードをここに追加
} else {
    alert("<%= escape_javascript(@notification.errors.full_messages.to_sentence) %>");
    // エラーメッセージを表示するためのJavaScriptコードをここに追加
}
