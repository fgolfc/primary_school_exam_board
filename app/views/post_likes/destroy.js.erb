$("#like-button-<%= @post.id %>").html('<%= j link_to "❤️ いいね", post_post_likes_path(post_id: @post.id), method: :post, remote: true, class: "btn custom-like-btn small-font", data: { post_id: @post.id } %>');
$("#like-count-<%= @post.id %>").text('<%= @post.post_likes.size %>');
